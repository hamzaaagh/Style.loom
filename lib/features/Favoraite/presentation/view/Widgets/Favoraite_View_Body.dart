import 'package:flutter/widgets.dart';

class FavoraiteViewBody extends StatelessWidget {
  const FavoraiteViewBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}